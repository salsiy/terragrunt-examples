generate_projects:
  blocks:
    - block_name: dev-us-east-1
      terragrunt: true
      root_dir: live/dev-account/us-east-1
      workflow: default
      workflow_file: digger-dev-us-east-1.yml
      include_patterns:
        - live/dev-account/app-config.yaml


workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - init
        - apply